# AI Infra Theory T6：Stable Softmax、Cross Entropy 与数值稳定性

> 版本：2026-09-24，统一理论讲解标准重写版
> 当前进度：T1~T3 已通过；T6 应在 T4、T5 后学习
> 建议投入：约 4~6 个有效小时
> 固定产出：`stable_softmax.py`
> 环境：[ENVIRONMENT.md](../ENVIRONMENT.md) 中的 Python 3.12 + NumPy

---

# Part 1：一个数学上正确、计算机里却会坏掉的公式

## 0. 今天要修复什么

分类模型可能输出：

```text
[1000.0, 1001.0, 1002.0]
```

这三个数还不是 probabilities，而是模型给三个 classes 的 raw scores。这种对象叫 logits（未经概率归一化的原始分类分数）。

我们希望把 logits 转成一组非负、总和为 `1`、保留 score 排序的 probabilities。Softmax 的数学公式能做到，但若直接计算 `exp(1002)`，`float64` 也会 overflow（上溢，结果超过可表示范围）。

今天把这条链串清：

```text
logits
-> softmax probabilities
-> correct-class probability
-> cross-entropy loss
```

同时回答：为什么同一个数学公式，必须换一种等价写法才适合 floating-point computer。

## 1. Softmax 为什么自然出现

指数函数满足 $e^z>0$，先对每个 score 取指数：

$$
u_i=e^{z_i}
$$

再除以所有指数之和：

$$
p_i
=
\frac{e^{z_i}}
{\sum_{j=1}^{C}e^{z_j}}
$$

$C$ 是 class count。于是：

$$
p_i>0,
\qquad
\sum_{i=1}^{C}p_i=1
$$

这个 operation 叫 softmax（软最大归一化）。它把 scores 映射成 categorical probabilities，但不会把最大项强行变成 `1`。

## 2. 数学正确为什么不等于数值安全

直接照公式实现：

```python
def naive_softmax(logits: np.ndarray) -> np.ndarray:
    exponentials = np.exp(logits)
    return exponentials / np.sum(exponentials)
```

输入 `[1,2,3]` 时通常工作。输入 `[1000,1001,1002]` 时：

```text
np.exp(1000) -> inf
inf / inf      -> nan
```

`inf` 是 infinity（无穷表示）；`nan` 是 not-a-number（未定义数值结果）。问题不在 softmax 定义，而在 intermediate values 超出 dtype 范围。

另一端也可能 underflow（下溢）：极小正数被舍入成 `0`。若随后计算 `log(0)`，结果会变成 `-inf`。

数值程序必须同时关心：

```text
最终数学值
中间值是否 representable
等价表达在 floating-point 下的误差
```

## 3. 找到稳定写法所需的不变量

给所有 logits 同时加 constant $c$：

$$
\frac{e^{z_i+c}}
{\sum_j e^{z_j+c}}
=
\frac{e^ce^{z_i}}
{e^c\sum_j e^{z_j}}
=
\frac{e^{z_i}}
{\sum_j e^{z_j}}
$$

因此：

$$
\operatorname{softmax}(\mathbf z)
=
\operatorname{softmax}(\mathbf z+c)
$$

这叫 translation invariance（整体平移不变性）。我们可以先把整组 logits 平移到更安全的范围，再取指数。

先停在这里。你已经知道：

```text
所有 logits 必须减同一个值
平移后 probabilities 不变
希望平移后的最大 exponent 不再溢出
```

## 4. Batch 情况先明确 axis

若 logits shape 是 `[B,C]`：

```text
B：batch size
C：class count
```

每一行是一条 sample 的 class scores。归一化必须沿 class axis，即 `axis=-1`：

$$
\sum_{c=1}^{C}p_{b,c}=1
\quad
\text{for every }b
$$

若沿 batch axis 归一化，数值也可能总和为 `1`，但含义变成“同一 class 在不同 samples 间竞争”。Axis 是 correctness contract，不只是 NumPy 参数。

## 5. 开工前只需要这些 API

```python
np.exp(values)
np.max(values, axis=-1, keepdims=True)
np.sum(values, axis=-1, keepdims=True)
np.log(values)
np.isfinite(values)
np.testing.assert_allclose(actual, expected, rtol=..., atol=...)
```

`keepdims=True` 保留 reduction axis 为长度 `1`，让 `[B,1]` 可以 broadcast 回 `[B,C]`。这里只解释 shape，不给稳定函数的完整组合。

# Part 2：Round1，独立找到稳定 probability path

## 6. 程序用途与 contract

文件：`stable_softmax.py`

同一组 logits 运行两条路径：

```text
naive path：直接 exp -> sum -> divide
stable path：利用整体平移控制中间值
```

实现：

```python
def naive_softmax(logits: np.ndarray) -> np.ndarray:
    ...


def stable_softmax(logits: np.ndarray, axis: int = -1) -> np.ndarray:
    ...
```

输入：

```text
floating ndarray
至少 1-D
目标 axis 长度 > 0
所有 logits finite
```

输出：

```text
shape 与 input 相同
所有 values finite 且非负
沿 axis 的 sums 约等于 1
不修改 caller input
```

固定 cases：

```python
ordinary = np.array([1.0, 2.0, 3.0], dtype=np.float64)
extreme = np.array([1000.0, 1001.0, 1002.0], dtype=np.float64)
batch = np.array(
    [
        [1.0, 2.0, 3.0],
        [1000.0, 1001.0, 1002.0],
        [-1000.0, -1001.0, -1002.0],
    ],
    dtype=np.float64,
)
```

最低 evidence：

```text
ordinary 上 naive 与 stable 接近
extreme 上主动捕获 naive 的 non-finite
stable extreme 全 finite 且 sum == 1
batch 每行独立归一化
stable_softmax 不修改 input
整体加 500 后 probabilities 不变
```

Naive failure 是预期观察：

```python
with np.errstate(over="ignore", invalid="ignore"):
    naive_result = naive_softmax(extreme)
```

运行：

```bash
python -m py_compile stable_softmax.py
python stable_softmax.py
```

### Reading Gate

到这里停止。先完成 stable path，并记录：

```text
选择了什么 shift，为什么
ordinary / extreme / batch 的 shape 与 sums
translation-invariance comparison
input 是否保持不变
```

Round1 通过后再读下面的 mechanism 与 loss。

---

# Part 3：从 stable probability 继续走到 stable loss

## 7. 为什么减 maximum

对一组 logits 选择：

$$
m=\max_i z_i
$$

计算：

$$
p_i
=
\frac{e^{z_i-m}}
{\sum_j e^{z_j-m}}
$$

所有 shifted logits 满足 $z_i-m\le0$，最大的 exponential 正好是 $e^0=1$，其余项位于 $(0,1]$。这直接切断 positive overflow。

```text
[1000,1001,1002]
-> subtract 1002
-> [-2,-1,0]
-> exponentials all finite
```

减 mean 虽然也保持 softmax 不变，却不能保证最大 shifted value 小于等于 `0`。减 maximum 同时利用了不变量与指数范围。

Batch 中 maximum 必须逐行计算：

```python
maximum = np.max(logits, axis=axis, keepdims=True)
shifted = logits - maximum
```

## 8. 从 correct-class probability 推出 cross entropy

假设正确 class index 为 $y$，模型分给它的 probability 是 $p_y$。我们希望：

```text
p_y 接近 1 -> loss 接近 0
p_y 接近 0 -> loss 很大
```

Negative log-likelihood（负对数似然）满足：

$$
L=-\log p_y
$$

One-hot target 下的 cross entropy（交叉熵）是：

$$
L
=
-\sum_{c=1}^{C}y_c\log p_c
$$

只有正确 class 的 $y_c=1$，因此退化成 $-\log p_y$。其他 classes 仍通过 softmax denominator 影响 $p_y$。

## 9. Stable softmax 后直接 `log` 仍可能失败

若某项 probability 因 underflow 变成 `0`，`-np.log(0)` 仍是 `inf`。更稳定的路径是从 logits 直接计算 log probability。

定义 log-sum-exp：

$$
\operatorname{LSE}(\mathbf z)
=
\log\sum_j e^{z_j}
$$

利用 $m=\max_j z_j$：

$$
\operatorname{LSE}(\mathbf z)
=
m+\log\sum_j e^{z_j-m}
$$

于是：

$$
\log p_y
=
z_y-\operatorname{LSE}(\mathbf z)
$$

$$
L
=
\operatorname{LSE}(\mathbf z)-z_y
$$

这条路径不需要先 materialize（实体化）完整 probabilities 再取 log。Framework 的 fused cross-entropy kernel 也围绕相同数值思想组织。

## 10. Batch loss 的 shape

输入：

```text
logits [B,C]
labels [B]，integer class indices
```

每行得到一个 loss：

$$
\ell_b
=
\operatorname{LSE}(\mathbf z_b)-z_{b,y_b}
$$

最后沿 batch 求 mean：

$$
L
=
\frac{1}{B}\sum_{b=1}^{B}\ell_b
$$

Class reduction 与 batch reduction 是两次不同 operation。不要只写“对 logits 求和”而不说明 axis。

## 11. Round3：完成 stable loss reference

增加：

```python
def stable_logsumexp(logits: np.ndarray, axis: int = -1) -> np.ndarray:
    ...


def cross_entropy_from_logits(
    logits: np.ndarray,
    labels: np.ndarray,
) -> float:
    ...
```

Fixed case：

```python
logits = np.array(
    [
        [2.0, 1.0, 0.0],
        [1000.0, 1001.0, 1002.0],
    ],
    dtype=np.float64,
)
labels = np.array([0, 2], dtype=np.int64)
```

验证：

```text
logsumexp 与手工 stable reference 接近
loss finite 且为 scalar
给每行各自加 constant，loss 不变
提高 correct-class logit 后，该 sample loss 下降
invalid label / shape / non-finite input 明确失败
```

可用 `torch.nn.functional.cross_entropy` 作外部 reference，但 NumPy implementation 内部不能调用 PyTorch。被测路径与 oracle 共享实现，就不是独立证据。

## 12. 常见分布放回正确位置

```text
binary classification
-> one logit -> sigmoid -> Bernoulli probability

multiclass / next-token prediction
-> C logits -> softmax -> categorical probabilities

continuous target with Gaussian assumption
-> model predicts distribution parameters
-> Gaussian likelihood
```

T6 不展开完整 maximum-likelihood theory。只要看见：loss 常来自“让 observed target 在 model distribution 下更可能”。

# Part 4：收口

## 13. 为什么不把 epsilon 当通用修复

有人会写：

```python
safe_probability = np.clip(probability, 1e-12, 1.0)
loss = -np.log(safe_probability)
```

Clipping 可作为某些 API 的保护，但它改变了原 probability。若 logits 在手，stable log-sum-exp 是更忠实的路径。给 denominator 随便加 epsilon 同样改变 normalization。

数值稳定性的核心不是“看到除法就加小数”，而是找到数学等价、但 intermediate values 更安全的表达。

## 14. 通过标准

程序 evidence：

```text
naive path 对 extreme logits 可控暴露 non-finite
stable path 对 ordinary/extreme/batch 都 finite
每条 sample 沿 class axis sum == 1
translation invariance 成立
cross entropy 与独立 reference 接近
input 不被修改，invalid input 非零退出
```

理解 evidence：

```text
解释 logits 为什么不是 probabilities
推导 softmax translation invariance
解释为什么减 max
从 -log p_y 推到 logits-space stable loss
区分 class reduction 与 batch reduction
```

不要求 pytest、benchmark、GPU kernel、optimizer 或 softmax backward。

## 15. 与 AI Infra 的连接

LLM inference 中：

```text
hidden state
-> vocabulary projection
-> logits [batch,vocab]
-> temperature / filtering
-> stable normalization or log-probability
-> sampler
```

Vocabulary 很大，因此 softmax 同时涉及 reduction axis、intermediate Tensor bytes、kernel launch、memory bandwidth、fusion 与 distributed communication。当前先建立正确 reference；以后所有 fast path 都要能回到这里对质。

## 16. 视频与资料

Round1 前：

- 吴恩达 [P60 多类别](https://www.bilibili.com/video/BV1owrpYKEtP?p=60)、[P61 Softmax](https://www.bilibili.com/video/BV1owrpYKEtP?p=61)。

Round1 后：

- 吴恩达 [P62 Softmax 输出网络](https://www.bilibili.com/video/BV1owrpYKEtP?p=62)、[P63 改进实现](https://www.bilibili.com/video/BV1owrpYKEtP?p=63)。
- 李沐 [09 Softmax 回归与损失](https://www.bilibili.com/video/BV1K64y1Q7wu/)。
- 李沐 [14 数值稳定性](https://www.bilibili.com/video/BV1u64y1i75a/)。

视频是第二解释，不替代 executable evidence。

## 17. 压缩记忆

```text
logits 是 raw scores
softmax 把 class scores 变成 categorical probabilities
整体平移不改变 softmax
减 row maximum 让最大 exponent 变成 1
cross entropy 可从 logits 通过 log-sum-exp 稳定计算
axis 是 probability contract，也是系统 contract
```
